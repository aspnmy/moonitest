name = "aspnmy/moonitest"
version = "0.2.2"
readme = "README.md"
description = "MoonBit integration test framework - Docker lifecycle + TCP/HTTP via moodtcpcli"
license = "Apache-2.0"
repository = "https://github.com/aspnmy/moonitest"
keywords = ["integration-test", "docker", "testing", "linux", "native"]
preferred_target = "native"

import {
  "aspnmy/moodtcpcli@0.1.0",
}
